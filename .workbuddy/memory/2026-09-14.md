# 2026-09-14

## X Copilot 项目启动
- Review 了 PROJECT.md（Chrome Extension AI Copilot for X，MVP = Tweet 识别 → 5 条评论生成 → 填入 Reply，绝不自动发送）。
- 输出技术方案 `docs/ARCHITECTURE.md`，关键决策：
  - 工具链 WXT + React 18 + TS，Panel 用注入式 Shadow DOM（不用 chrome.sidePanel）
  - LLM 请求由 background SW 代理（CORS + key 保护），MVP 直连 OpenAI 兼容 endpoint，不建 backend
  - TweetDetector：URL 主判 + DOM 提取（data-testid）
  - Fill Reply 用 execCommand('insertText')，定为全项目最高风险项
  - 新增 Task 0 spike：最先真机验证 fill composer，通过后再搭 UI
- 下一步：按修订任务顺序开工（Task 0 → 1 脚手架）。
- LLM 选型确定：DeepSeek（OpenAI 兼容协议，base_url https://api.deepseek.com）。当前模型为 deepseek-flash（默认）和 deepseek-v4-pro（备选），以 https://api-docs.deepseek.com/zh-cn/ 为准，强制 JSON 输出。
- MVP 脚手架完成并 build 通过（git ab16c49）：WXT + React + Shadow DOM Panel + DeepSeek 后台代理 + 静态 Options 页。踩坑：Vite 在中文路径不产出 HTML 入口（对策：HTML 放 public/ 静态实现）；WXT 会执行 entrypoints/ 顶层裸 .tsx。
- 用户反馈：右键插件图标只有浏览器菜单，设置入口改为 Panel 左下角齿轮（OPEN_OPTIONS 消息走 background openOptionsPage），已提交 2f5dd22。
- 用户反馈：Options 页点保存无反应。根因：MV3 CSP（script-src 'self'）禁止扩展页面内联 script，脚本被静默忽略。修复：JS 拆成外部 public/options.js。教训：public/ 静态 HTML 页在 MV3 里也不能用内联 script。
- ✅ 里程碑：用户真机验证端到端跑通（生成 + 填入 Reply 框均正常）。Task 0 spike 验收通过，execCommand('insertText') 方案有效。用户将继续体验并汇总交互细节 bug，待其反馈后修复。
- 用户反馈两个 fill bug（同一条重复两次 + 影子残留；连续填入叠加而非替换）。根因：execCommand('insertText') 在 X 的 Lexical 编辑器上双重触发。修复：改用合成 paste 事件（Lexical 单次接管），填入前 selectAll+delete 实现替换语义，校验改为 textContent 与目标完全一致；App 侧 filledIds 改单值 filledId。
- 交互升级（用户要求）：进入/切换 Tweet 自动弹出 Panel 并自动生成（缓存命中不重复请求）；加生成序号 guard 防止生成中切帖结果错挂；自动触发有转圈 + toast 提示。用户删除了自己创建的空 bugs.md。
- 用户新增需求：x.com/home 里点评论图标弹出的 Reply Modal 不改 URL，原 URL 主判 Detector 失效。已加 Modal 兜底：URL 无 status 时从可见 dialog 的 tweet article 提取被回复帖（a[href*=/status/] 拿 id/handle）；Modal 关闭时取消在途生成并收起 Panel（提交 a5de818）。详情页内联/Modal 填入用户已验证兼容。
- 用户实测 /home Reply Modal 检测未生效。已加固：dialog 选择器补 [aria-labelledby=modal-header]；status 链接缺失时用 handle+datetime+文本哈希合成稳定 id；加 [X Copilot] console.debug 日志（提交 ef87262）。待用户刷新 x.com 页面（扩展刷新后 content script 不会自动注入已开标签页）再测，并回报 Console 日志。
- ✅ /home Reply Modal 用户确认可用。新 bug：详情页点评论图标会重复触发生成。根因：Modal 挂载瞬态导致页面瞬间无 article → Detector 误判离开（null）→ 取消在途生成（未入缓存）→ 同 id 重新检出 → 真生成。修复：null 判定加 400ms 确认期（提交 32c0441）。

