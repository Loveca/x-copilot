# 2026-09-14

## X Copilot 项目启动
- Review 了 PROJECT.md（Chrome Extension AI Copilot for X，MVP = Tweet 识别 → 5 条评论生成 → 填入 Reply，绝不自动发送）。
- 输出技术方案 `docs/ARCHITECTURE.md`，关键决策：
  - 工具链 WXT + React 18 + TS，Panel 用注入式 Shadow DOM（不用 chrome.sidePanel）
  - LLM 请求由 background SW 代理（CORS + key 保护），MVP 直连 OpenAI 兼容 endpoint，不建 backend
  - TweetDetector：URL 主判 + DOM 提取（data-testid）
  - Fill Reply 用 execCommand('insertText')，定为全项目最高风险项
  - 新增 Task 0 spike：最先真机验证 fill composer，通过后再搭 UI
- 下一步：按修订任务顺序开工（Task 0 → 1 脚手架）。
- LLM 选型确定：DeepSeek（OpenAI 兼容协议，base_url https://api.deepseek.com）。当前模型为 deepseek-flash（默认）和 deepseek-v4-pro（备选），以 https://api-docs.deepseek.com/zh-cn/ 为准，强制 JSON 输出。
- MVP 脚手架完成并 build 通过（git ab16c49）：WXT + React + Shadow DOM Panel + DeepSeek 后台代理 + 静态 Options 页。踩坑：Vite 在中文路径不产出 HTML 入口（对策：HTML 放 public/ 静态实现）；WXT 会执行 entrypoints/ 顶层裸 .tsx。
- 用户反馈：右键插件图标只有浏览器菜单，设置入口改为 Panel 左下角齿轮（OPEN_OPTIONS 消息走 background openOptionsPage），已提交 2f5dd22。
- 用户反馈：Options 页点保存无反应。根因：MV3 CSP（script-src 'self'）禁止扩展页面内联 script，脚本被静默忽略。修复：JS 拆成外部 public/options.js。教训：public/ 静态 HTML 页在 MV3 里也不能用内联 script。
- ✅ 里程碑：用户真机验证端到端跑通（生成 + 填入 Reply 框均正常）。Task 0 spike 验收通过，execCommand('insertText') 方案有效。用户将继续体验并汇总交互细节 bug，待其反馈后修复。
- 用户反馈两个 fill bug（同一条重复两次 + 影子残留；连续填入叠加而非替换）。根因：execCommand('insertText') 在 X 的 Lexical 编辑器上双重触发。修复：改用合成 paste 事件（Lexical 单次接管），填入前 selectAll+delete 实现替换语义，校验改为 textContent 与目标完全一致；App 侧 filledIds 改单值 filledId。
- 交互升级（用户要求）：进入/切换 Tweet 自动弹出 Panel 并自动生成（缓存命中不重复请求）；加生成序号 guard 防止生成中切帖结果错挂；自动触发有转圈 + toast 提示。用户删除了自己创建的空 bugs.md。
- 用户新增需求：x.com/home 里点评论图标弹出的 Reply Modal 不改 URL，原 URL 主判 Detector 失效。已加 Modal 兜底：URL 无 status 时从可见 dialog 的 tweet article 提取被回复帖（a[href*=/status/] 拿 id/handle）；Modal 关闭时取消在途生成并收起 Panel（提交 a5de818）。详情页内联/Modal 填入用户已验证兼容。
- 用户实测 /home Reply Modal 检测未生效。已加固：dialog 选择器补 [aria-labelledby=modal-header]；status 链接缺失时用 handle+datetime+文本哈希合成稳定 id；加 [X Copilot] console.debug 日志（提交 ef87262）。待用户刷新 x.com 页面（扩展刷新后 content script 不会自动注入已开标签页）再测，并回报 Console 日志。
- ✅ /home Reply Modal 用户确认可用。新 bug：详情页点评论图标会重复触发生成。两次修复（32c0441 null 确认期 / 964cffd 幂等保护）均无效。按用户要求回滚，问题记录在 docs/TODO.md（含 H1 URL 变体 / H2 article 提取错位 / H3 detector effect 重建三个假设与排查步骤）。
- ⚠️ 事故：执行 git revert 时 .git 目录意外丢失（原因不明），全部提交历史丢失。已重新 git init，从对话记录重放全部历史提交（代码文件逐版本还原，消息带 [replayed] 标记）。教训：对同一目录连续做 stash/revert 等多步 git 操作前，先确认每步成功；本机环境在命令被 SIGTERM 打断时会丢文件写入，git 操作务必逐条执行并验证；重要历史应尽快 push 到远端。
- 待办优先级：docs/TODO.md 的详情页重复生成 bug；用户下一次测试时先收集 [X Copilot] console 日志序列再改代码。
- 恢复结果：9 个提交全部从对话记录重放完毕（889d751 → 9c5c02e），代码文件逐版本还原，工作区干净，`npm run build` 通过（产物 198.63 kB）。期间 node_modules 的 .bin shim 与 async-mutex 等散件被环境丢失导致 build 失败，`npm install` 修复（.bin 恢复 93 个项）。
- 环境风险提示：本机环境在命令被 SIGTERM 打断或批量删除被守卫拦截时会丢文件（已分别导致 .git 消失、public/ 目录消失、node_modules 局部损坏）。建议：git 操作逐条执行并验证；重要历史尽快 push 到远端仓库做备份。
- Git 身份与远端：身份改为 Loveca <Loveca@users.noreply.github.com>（filter-branch 重写全部历史）；origin = https://github.com/Loveca/x-copilot.git，master 已推送（86e2327）。github.com 直连不通，需本地代理 127.0.0.1:7897，已配仓库级 http.https://github.com.proxy；曾因 Windows 凭据管理器里的失效凭据导致 push 失败，`git credential reject` 清除后成功。
- UI 主题反复：先做暗色（X dark）→ 用户说明自己用 X 浅色模式 → 改为白底黑字（#fff/#f7f9f9/#0f1419/#536471，主按钮黑底白字）。教训：涉及主题/配色要先问用户用哪套模式，别自己猜。
- 交互修复：Panel 打开时悬浮球改为淡出隐藏（scale 0.6 + opacity 0 + pointer-events none），关闭面板后恢复原位置（提交 24f9167）。
- ✅ 远端改为 SSH：本机早有可用 SSH 配置（~/.ssh/id_ed25519_github，ssh -T git@github.com → 认证为 Loveca，22 端口直连可用）。origin 已切换为 git@github.com:Loveca/x-copilot.git，仓库级 HTTPS 代理配置已移除。**教训：配置远端前先跑 `ssh -T git@github.com`（一条命令）判断 SSH 是否可用，不要默认走 HTTPS+GCM。**
- 设置页改版：左侧导航 + 右侧分区（模型配置 / 回复风格 / 自动生成 / 插件信息）。「自动生成」做成真功能，存 `uiConfig`，Panel 用 storage.onChanged 即时生效。
- 回复风格可配置：`uiConfig.styles`，支持排序（先试拖拽失败 → 改上下箭头）、每种 1-3 条、启用开关、恢复默认。**排序失效的真根因**：`normalizeStyles` 遍历默认清单按 key 查存储，输出顺序永远是默认顺序，用户排的序在读取时被丢弃（count 是条目属性所以不受影响）。修复：以存储顺序为准，默认清单新增项追加末尾；config.ts 与 options.js 两处同改。
- 用词统一：UI / README / ARCHITECTURE 的「评论」全部改为「回复」（X 官方中文用词），docs/PROJECT.md 保持原样留档。
- 意图输入框：Panel 可选输入「我想说什么」，透传 `options.intent`，缓存键 `tweetId::intent`。
- 流式生成：输出改 NDJSON，`runtime.connect` 长连接边收边推，面板逐条渲染。
- **延迟问题定位（重要）**：实测「首字节 0.2s · 首条 12.5s · 完成 12.9s」——连接瞬时完成、12 秒内无任何正文字符、最后 0.4 秒一次性出完。根因是 **DeepSeek V4 思考模式默认开启且 reasoning_effort=high**，先流一大段 reasoning_content（我们此前直接忽略）。修复：请求体加 `thinking:{type:'disabled'}`（默认关，设置页「深度思考」可开回）；同时把埋点拆成 首字节/出字/首条/完成 四项 + 服务端模型名 + 思维链字数，生成中加实时秒表与「模型思考中 N 字」，并把流式解析从「按换行」改为「按大括号配对」。提交 dd8af75。
- ✅ 修复已验证：改后「首字节 0.3s · 出字 0.6s · 首条 0.8s · 完成 1.6s，模型 deepseek-flash，正文 303 字」——**12.9s → 1.6s，8×**。网络自始不是瓶颈（首字节始终 ~0.2s）。教训：遇到「连接快但迟迟没有正文」的流式表现，先怀疑思考/推理模式，别急着怪网络或换服务商。

